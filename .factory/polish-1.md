# Polish 1 — review-1 acceptance map

All findings in `.factory/review-1.md` were treated as release work. Evidence
below is generated by the clean-clone claim and browser suite recorded in the
handoff, plus the deployed live-site check.

| Finding | Change made | Evidence |
|---|---|---|
| F-1-1 | Declared `rust-version = "1.88"`, corrected README, and added the MSRV claim. | `@claim:msrv-build` runs `rustup run 1.88.0 cargo build --locked`. |
| F-1-2 | Removed the unbounded “safe” wording from the headline, README, and art alt text. | Copy audit; `npm test`. |
| F-1-3 | Split the default-demo copy and added an observable default-demo claim. | `@claim:default-cli-demo` parses the folder and mock command, starts it, and verifies the fixture. |
| F-1-4 | Route renders now update Open Graph and Twitter titles/descriptions; standalone 404 has full social metadata. | `routes update social metadata and the standalone 404 shares the site chrome`. |
| F-1-5 | Rebuilt the static 404 with the same BR mark, three-link nav, footer one-liner, builder link, version, and build id. | Static-404 chrome regression; live `/missing-route` check. |
| F-1-6 | Rewrote the first-screen audience sentence around queue, webhook, and third-party failures. | Copy audit and desktop/mobile first-read tests. |
| F-1-7 | Renamed the preview index to describe the fixture outcome. | Copy audit; landing screenshot. |
| F-1-8 | Renamed the preview heading to describe scrubbing before export. | Copy audit; landing screenshot. |
| F-1-9 | Renamed the workflow index to name capture, scrubbing, and replay. | Copy audit; landing screenshot. |
| F-1-10 | Renamed the limits index to state what the product will not do. | Copy audit; landing screenshot. |
| F-1-11 | Replaced `PII` and `field(s)` with plain wording in the site and CLI output. | `@claim:default-cli-demo`; copy audit. |
| F-1-12 | Added a labelled selectable install-command field and a usable copy-failure recovery message. | Install-guidance browser regression. |
| F-1-13 | Rewrote the offline notice in visitor language. | `@claim:offline-demo`. |
| F-1-14 | Split the README deployment sentence into direct-load and 404 sentences. | Copy audit review; README. |
| F-1-15 | Replaced “release-ready” with the exact locked package verification command. | README; `cargo package --allow-dirty --locked`. |
| F-1-16 | Replaced the standalone and SPA 404 metaphors with “Page not found” and “Return home.” | Routing/404 browser regression and live missing-route check. |

## Live evidence

Deployed production URL: https://incident-boundary-replay.sociobot.in

- Desktop first-screen screenshot:
  `.factory/qa-evidence/polish-1-live-desktop.png`.
- Mobile isolated-demo screenshot:
  `.factory/qa-evidence/polish-1-live-demo-mobile.png`.
- `/`, `/demo`, `/privacy`, and `/terms` return HTTP 200; `/missing-route`
  returns HTTP 404. Each has the expected title, one `h1`, one `main`, no
  missing image alt text, route-correct social metadata, same-origin requests,
  no page errors, and zero serious/critical Axe findings. Raw result:
  `/tmp/ibr-polish-live-check.json`.

Additional required demo coverage: `?demo=1` remains a direct isolated demo
entry and is regression-tested with its banner, reset, and exit controls.
